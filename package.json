{
  "name": "smallworld",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @smallworld/web dev",
    "dev:api": "cd apps/api && uv run uvicorn smallworld_api.main:app --reload --host 0.0.0.0 --port 8000",
    "lint:web": "pnpm --filter @smallworld/web lint",
    "test:api": "cd apps/api && uv run pytest"
  }
}
